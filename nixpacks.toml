[phases.setup]
nixPkgs = ['python3', 'gcc', 'nodejs_20']

[phases.install]
cmds = [
  'python3 -m venv /opt/venv',
  '/opt/venv/bin/pip install -e ".[all]"'
]

[phases.build]
cmds = [
  'cd /app/frontend && npm install && npm run build'
]

[start]
cmd = '/opt/venv/bin/opencmo-web'
