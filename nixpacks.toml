[phases.setup]
nixPkgs = ['python3', 'gcc', 'nodejs_20']

[phases.install]
cmds = ['echo "skip"']

[start]
cmd = 'pip install -e ".[all]" && cd frontend && npm install && npm run build && cd .. && opencmo-web'
